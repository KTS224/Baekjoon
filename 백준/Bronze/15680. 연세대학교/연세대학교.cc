#include <iostream>
using namespace std;

int main() {
    
    int num;
    scanf("%d",&num);
    
    if (num == 0)
        printf("YONSEI");
    else if (num == 1)
        printf("Leading the Way to the Future");
    return 0;
}